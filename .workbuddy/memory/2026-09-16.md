# 2026-09-16

## 撤销 git 工作区全部改动（D:\workagent）

- 初始状态：18 个已跟踪文件被删除（无修改、无未跟踪文件），涉及 `apps/desktop/src/main/{app,storage}.ts`、`apps/desktop/src/renderer/{App.tsx,styles.css,core/thread-ui-state.ts,lib/config-utils.ts,settings/pages/knowledge/memory-page.tsx}`、`docs/MODIFICATION_LOG.md`、`packages/{agent-runtime,shared-types}/src/index.ts`、`tests/thread-ui-state.test.ts`、`.codexh/outputs/.../context/*.md`、`.workbuddy-ai/memory/2026-09-16.md`。
- 执行 `git restore .` 前遇到 `fatal: Unable to create '.git/index.lock'`：锁文件 0 字节、已存在约 7.4 小时、无 git 进程占用 → 判定为崩溃遗留的僵尸锁，手工删除后恢复成功。
- 结果：`git status` 为 clean，stash 为空，HEAD 仍为 `677092f`。

## 环境坑（重要，会复现）

- 本机 Bash 工具的 PATH 是坏的：`ls` / `head` / `dirname` 等均 `command not found`，**只有 `git` 命令可用**。需要列目录/管道时改用 Glob / Grep / Read。
- PowerShell 工具的 stdout 会被吞掉（返回 exit code 0 但无输出）→ 探测类命令必须把结果 `Set-Content` 写到临时文件再用 Read 读取，并在结束后清理临时文件。
- Bash 里可用托管 Python：`C:/Users/xhwange/.workbuddy/binaries/python/versions/3.13.12/python.exe -c "..."`（无 `head`，输出长度自己在脚本里截断）。
- **同一文件的多个 Edit 调用不能并行发**：一次消息里发两个 Edit（同一文件）会相互覆盖，工具都回success 但只有一个生效（本次连中三次：接口字段、调用点、测试 fixture 各丢一次；vitest 因为类型被擦除照样全绿，只有 tsc 能暴露）。要么串行发，要么改完立刻 Grep 复核。

## 诊断：codexh 报「400 credit insufficient balance」（provider 额度耗尽）

- 结论：不是 codexh 的 bug，是上游网关（provider-16 / `qwen3.8-flash-free`）返回 HTTP 400 `code=insufficient_user_quota`，账户余额 79（一度为 0）< 本次预扣 4368。模型名带 `-free` 仍按额度预扣。
- 会话 `9c26438d-5986-481d-a4b8-6263b74b4b09`（chat 模式）里 6 次 `turn.failed`，全部同一原因，分布在 09:01:55 / 09:04:15 / 09:04:35 / 09:04:43 / 09:10:28 / 09:10:33（UTC）。
- 失败请求规模：36 条 message(140.9KB) + system prompt 64.9KB + 57 个 tool schema(21.2KB) → payload 160,910 bytes。期间已自动 compact 7 次，required 仍是 4368 → 固定开销（系统提示 + 工具定义）压不掉。
- provider-16 配置 `maxRequestBytes=0`、`maxTools=0` → codexh 本地字节闸门不触发，只有上游额度闸门拦截；上下文窗口 256000，与本次无关。
- 兜底文案来源：`packages/agent-runtime/src/index.ts:10464-10468`（通用「无法自动恢复」分支）；重试分类 `isUpstreamServiceUnavailableError`(593) 只认 500/502/503/504，quota 类 400 不匹配任何重试分支 → 立即 fail，不重试。
- 排查入口：`~/.codexh/logs/provider-request-error-*.json`（完整请求体 dump，由 `packages/provider-adapters/src/index.ts:682 enrichProviderRequestError` 写入）+ `~/.codexh/logs/sessions/*.jsonl`（`turn.failed` 事件含完整错误串、`agent.context_measured` 含 providerId/modelId/contextWindow）。

## 优化 provider 报错的用户可见文案（已落地）

用户反馈报错「一堆乱七八糟、看不明白」。改动四处：

- 新增 `packages/agent-runtime/src/provider-failure-message.ts`：`classifyProviderFailure` 把 provider 故障分成 quota / auth / model_unavailable / context_length / request_too_large / content_filter / rate_limit / service_unavailable / network，`buildProviderFailureMessage` 输出「结论 + 原因 + 建议 + 一行技术细节 + dump 路径」。`summarizeFailureDetail` 负责给兜底文案去尾部、压空白、截断。
- `packages/provider-adapters/src/index.ts`：诊断尾巴加 `provider=`、role **直方图**（`roles=system:1,user:5,assistant:30`，取代原全序列）、跳过空 `param=`/`type=`；新增导出 `ProviderRequestDiagnostics` / `readProviderRequestDiagnostics` / `stripProviderRequestDiagnostics`；结构化诊断挂到 error 上（非枚举属性）。**上游 code 可能是数字（`code: 11133`），必须按 string|number 处理**，首版漏了这个导致 code 丢失。
- `packages/agent-runtime/src/index.ts`：`buildRuntimeFailureRecoveryMessage` 在所有既有分支之后接入 provider 分类，兜底改为「结论 + 脱敏后的原因 + 技术细节行」；新增 `providerErrorMessage` 统一 strip 尾部，供状态码判定函数使用。
- 顺带修掉一个潜在 bug：尾部含裸整数（`bytes=500`/`messages=429`）会被 `isUpstreamServiceUnavailableError`、`isModelRateLimitError`、`isUpstreamContextOverflowError`、`isFunctionCallProtocolError` 的数字正则误判成 HTTP 状态码 → 无意义重试 5 次。

验证：`vitest run tests/agent-runtime.test.ts tests/provider-adapters.test.ts` → 491 passed；全量套件 1150 passed / 9 failed，失败全在 `tests/git-service.test.ts`（5s 超时跑真实 git 子进程，本机 shell PATH 坏导致，与本次改动无关）。
`pnpm typecheck`（tsc on apps/desktop）在本仓库**本来就不干净**（storage.ts / App.tsx / cheerio / findLastIndex 等约 30 处历史报错）；本次改动落在的行号区间内无任何报错。

## 文案里用「供应商名字」替代内部 id（已落地）

用户要求：报错里的 `provider-16` 换成供应商名字。真实配置 `C:\Users\xhwange\.codexh\config.toml` 里 `[providers.provider-16] name = "buzzai"`（其余：provider-9 商汤、provider-11 Buddy、provider-15 公司、provider-17 DeepSeek…），即 `name` 一直存在，只是文案没用它。

- `packages/provider-adapters/src/index.ts`：`ProviderRequestDiagnostics` 新增 `providerName`；`enrichProviderRequestError` 的 identity 增加 `providerName`，两个调用点（stream 963 / 非 stream 1108）都传 `input.provider.name`；尾部新增**带引号**的 `providerName="buzzai"`（名字里的空白折叠、`"` 与 `]` 去除，否则会提前截断尾部），由新的 `readQuotedField` 解析回结构化对象。
- `packages/agent-runtime/src/provider-failure-message.ts`：新增 `providerLabel(details) = providerName || providerId`；quota 原因的账户表述改为 `供应商「buzzai」的账户`、建议改为 `先给「buzzai」的账户充值`；技术细节行保留可对账的 id：`供应商 buzzai（provider-16）`（无名时退化为 `供应商 provider-99`）。
- 确认 provider 名字链路上没被剥掉：`config.toml` → `AppConfig.providers` → `resolveProtocolRecoveryProvider`（原样返回）→ `adapter.runTurn({provider})`；desktop 侧 `storage.ts` 只有 transport/deepseekProtocol 两个废弃字段被剔除。
- 测试：`tests/agent-runtime.test.ts` 新增「names the configured provider instead of leaking its internal id」（断言 原因 行不含 id）；`tests/provider-adapters.test.ts` 的 provider fixture 加 `name`，另加「sanitizes provider names that would break the diagnostics tail」（名字含 `"`/`]`/换行）。
- 验证：两个测试文件 493 passed；全量 1152 passed / 9 failed（仍是 git-service 超时）；tsc 报错行数 66 → 60，新增的 6 条全是并行 Edit 丢改动造成的假报错，修好后归零。

**没做的事**（用户若继续要求）：技术细节里的模型仍是 id（`qwen3.8-flash-free`）而不是 `displayName`；auth / model_unavailable / rate_limit 等其它 kind 的建议文案里仍未点名供应商。
